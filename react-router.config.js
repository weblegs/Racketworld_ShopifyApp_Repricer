/** @type {import('@react-router/dev/config').Config} */
export default {
  // Disable type generation — not needed for JSX projects, avoids Windows EPERM on .react-router/types
  typesafety: "none",
};
